Git hub is just like a cloud for codes. Recommended to load codes there for sharing.
There is limit per file size for github (spoken "jithub")
For python, anything in double quote are strings, shortform is str
Use print(type(name)) to check type of data
add cd if want to add subfolder, and cd..to go back up one folder
Index will always start at 0
Strip will remove the space before and after the variable
IO validate is to validate your data
Break will mean it will get out of that particular logic (settled because condition is met) then go to next logic
Dont nest if else too deep, stop until children (Dont nest too much)
While loop runs until a condition will be false
Sets are used when u dont care about the order of data
Lists is used if you care about order about data (indexes)
Project must be CRUD- create, read (view), update and delete a user, share about most outstanding crud and what would be the future enhancements to be done to the program
Project localhost je takyah online and must be fullstack
Presentation will be on 30th and 31st July
Mid next week or end this week will complete the syllabus
End week to propose on the project topic
2 mock presentations before the final day
Include an AI coach or AI insights
If use.add, it adds unique value
def means function (which means or refers to a formula)
return will return result for the specific formula or logic
so save the value of the formula in a variable print(resultofformula)
Nanti go back to set and dictionaries 
Read errors from bottom to top, but fix from top to bottom
